# Reviewable Workflows

This document captures which routing flows in the current `help-prototype` are strong enough to review with stakeholders after the latest routing-honesty cleanup.

## A. Workflows that are now complete enough to review end-to-end

| Workflow | Starting surface | Path through the prototype | Destination type(s) | Review status | Note |
|---|---|---|---|---|---|
| Reset password for direct Cengage sign-in | `#homepage`, `#sign-in-account`, `#search-results` | `Reset password` -> `#reset-password-chooser` -> `With a Cengage account` -> `#help-article?article=reset-cengage-password` | guided routing page -> help article | end-to-end | The chooser narrows by sign-in pathway and lands on a specific internal article. |
| Reset password through LMS | `#homepage`, `#sign-in-account`, `#search-results` | `Reset password` -> `#reset-password-chooser` -> `Through my LMS` -> `#reset-password-lms` | guided routing page -> instruction-path page | end-to-end | The path stays pathway-specific and does not bounce back to a broad hub. |
| Manage a Cengage account | `#sign-in-account`, `#sign-in-account?product=mindtap`, `#sign-in-account?product=webassign`, `#sign-in-account?product=sam`, `#search-results` | `Manage account` -> `#manage-account-chooser`, `#manage-account-chooser?product=mindtap`, `#manage-account-chooser?product=webassign`, or `#manage-account-chooser?product=sam` -> `I need to update my Cengage account` -> `#help-article?article=manage-account` | guided routing page -> help article | end-to-end | Shared surfaces use the shared chooser; MindTap-, WebAssign-, and SAM-scoped account surfaces now use product-scoped choosers so Spark-only branches do not appear in those product-context paths. |
| Manage a Spark account | `#sign-in-account`, `#sign-in-account?product=spark`, `#search-results`, `#product-hub-spark` | `Manage account` -> `#manage-account-chooser` or `#manage-account-chooser?product=spark` -> `I need to update my Spark account` -> `#help-article?article=spark-manage-account`; Spark `Account settings` -> `#manage-account-chooser?product=spark` -> same Spark-specific branch | guided routing page -> help article | end-to-end | Shared surfaces can still use the shared chooser, while Spark-scoped account surfaces now use the Spark-scoped chooser so they stay inside Spark account logic. |
| Manage a school-managed Spark account | `#homepage`, `#sign-in-account`, `#sign-in-account?product=spark`, `#search-results`, `#product-hub-spark` | `Manage account` -> `#manage-account-chooser` or `#manage-account-chooser?product=spark` -> school-managed Spark branch -> `#help-article?article=spark-school-managed-account`; Spark `Account settings` -> `#manage-account-chooser?product=spark` -> same Spark-specific branch; `#search-results` -> `#help-article?article=spark-school-managed-account` | guided routing page -> help article; search results -> help article | end-to-end | Shared and Spark-scoped account surfaces now both reach the truthful school-managed Spark article without widening into non-Spark account branches. |
| Wrong account for direct Cengage access | `#homepage`, `#sign-in-account`, `#sign-in-account?product=mindtap`, `#sign-in-account?product=webassign`, `#sign-in-account?product=sam`, `#course-access-enrollment?product=webassign`, `#course-access-enrollment?product=sam`, `#troubleshooting-common-problems?product=mindtap`, `#troubleshooting-common-problems?product=webassign`, `#troubleshooting-common-problems?product=sam`, `#search-results`, `#product-hub-mindtap`, `#product-hub-webassign`, `#product-hub-sam` | `Wrong account` -> `#wrong-account-chooser`, `#wrong-account-chooser?product=mindtap`, `#wrong-account-chooser?product=webassign`, or `#wrong-account-chooser?product=sam` -> direct Cengage-account branch -> `#help-article?article=wrong-account` or `#help-article?article=forgot-username` | guided routing page -> help article | end-to-end | MindTap-, WebAssign-, and SAM-scoped help-domain surfaces now use product-scoped choosers so Spark-only school/LMS branches do not appear in those product-context paths. |
| Wrong account for Spark sign-in | `#sign-in-account?product=spark`, `#course-access-enrollment?product=spark`, `#troubleshooting-common-problems?product=spark`, `#product-hub-spark` | `Wrong account` -> `#wrong-account-chooser?product=spark` -> `#help-article?article=spark-sign-in`, `#help-article?article=spark-forgot-username`, or the school-managed Spark branch depending on the option chosen | guided routing page -> help article | end-to-end | The Spark-scoped chooser stays inside Spark account logic and does not expose the higher-ed purchased-access branch used by MindTap, WebAssign, and SAM. |
| Wrong account for school-managed Spark access | `#homepage`, `#sign-in-account`, `#sign-in-account?product=spark`, `#course-access-enrollment?product=spark`, `#troubleshooting-common-problems?product=spark`, `#search-results`, `#product-hub-spark` | `Wrong account` -> `#wrong-account-chooser` or `#wrong-account-chooser?product=spark` -> school-managed Spark branch -> `#help-article?article=spark-school-managed-wrong-account`; `#search-results` -> `#help-article?article=spark-school-managed-wrong-account` | guided routing page -> help article; search results -> help article | end-to-end | Shared and Spark-scoped surfaces can both reach the truthful school-managed Spark account article; only the not-sure Spark branch remains unresolved. |
| LMS link not working for MindTap, WebAssign, or SAM | `#troubleshooting-common-problems`, `#course-access-enrollment?product=webassign`, `#course-access-enrollment?product=sam`, `#troubleshooting-common-problems?product=webassign`, `#troubleshooting-common-problems?product=sam`, `#search-results`, `#product-hub-mindtap`, `#product-hub-webassign`, `#product-hub-sam` | `LMS link not working` -> `#lms-link-chooser` or direct product-hub or product-scoped help-domain link -> `#help-article?article=lms-link-not-working` | guided routing page or product-hub link or product-scoped help-domain link -> help article | end-to-end | The shared chooser and product-specific shortcuts all land on the same truthful article. |
| Spark LMS link not working | `#homepage`, `#troubleshooting-common-problems`, `#course-access-enrollment?product=spark`, `#troubleshooting-common-problems?product=spark`, `#search-results`, `#product-hub-spark` | `LMS link not working` -> `#lms-link-chooser` -> `Spark` -> `#help-article?article=spark-lms-link-not-working`; Spark-scoped access or troubleshooting pages -> direct article; `#search-results` -> `#help-article?article=spark-lms-link-not-working` | guided routing page -> help article; product-scoped help-domain -> help article; search results -> help article | end-to-end | The Spark-specific broken-link branch now lands on a real product article instead of dead-ending, and Spark-scoped help-domain pages go straight to that article. |
| Join a Spark course from shared or Spark-scoped access surfaces | `#course-access-enrollment`, `#course-access-enrollment?product=spark`, `#search-results`, `#product-hub-spark` | `Enter course key`, `Redeem access code`, or `Join or enroll in a course` -> chooser -> Spark branch -> `#help-article?article=spark-join-course`; Spark-scoped `Join a course` -> direct article | help-domain hub or search -> guided routing page -> help article; product-scoped help-domain -> help article | end-to-end | The Spark access path now narrows to the real Spark join-course article instead of widening to the Spark hub, and the Spark-scoped access page goes straight there. |
| Spark LMS access | `#homepage`, `#course-access-enrollment`, `#course-access-enrollment?product=spark`, `#search-results` | `Access your course through LMS` or `LMS access help` -> `#lms-access-chooser` -> `I need Spark course access from my LMS or school platform` -> `#help-article?article=spark-lms-access`; Spark-scoped `Access your course through LMS or school platform` -> direct article; `#search-results` -> `#help-article?article=spark-lms-access` | help-domain hub -> guided routing page -> help article; product-scoped help-domain -> help article; search results -> help article | end-to-end | The institution-aware Spark access branch now lands on a specific article and stays out of the broad Spark hub, and the Spark-scoped access page goes straight there. |
| MindTap / Spark missing activities or assignments | `#homepage`, `#troubleshooting-common-problems`, `#troubleshooting-common-problems?product=spark`, `#search-results`, `#product-hub-mindtap`, `#product-hub-spark` | Homepage shortcut, Troubleshooting hub link, or `#search-results` result -> `#missing-activities-chooser` -> `MindTap` or `Spark` -> `#help-article?article=mindtap-missing-activities` or `#help-article?article=spark-missing-activities`; Spark-scoped troubleshooting page or MindTap/Spark product-hub shortcut -> direct article | filtered shortcut or help-domain hub or search -> guided routing page -> help article; product-scoped help-domain or product hub -> help article | end-to-end | The named MindTap and Spark branches now land on honest product-specific troubleshooting articles without widening back to a broad hub. |
| MindTap / WebAssign course-key entry | `#homepage`, `#course-access-enrollment`, `#course-access-enrollment?product=webassign`, `#search-results`, `#product-hub-mindtap`, `#product-hub-webassign` | `Enter course key` -> `#course-key-chooser` -> `MindTap` or `WebAssign` -> `#help-article?article=mindtap-course-key` or `#help-article?article=webassign-class-key`; WebAssign-scoped `Enter a class key` or `Join or enroll in a course` -> `#help-article?article=webassign-class-key`; `Register with a course key`, `Enroll with a class key`, or `Can't enroll with a class key` -> direct article | help-domain hub or search -> guided routing page -> help article; product-scoped help-domain or product hub -> help article | end-to-end | Shared access surfaces still use the chooser, while the WebAssign-scoped and product-hub shortcuts now go directly to the honest product-specific key-entry articles. |
| MindTap / WebAssign / SAM access-code redemption | `#homepage`, `#course-access-enrollment`, `#course-access-enrollment?product=webassign`, `#course-access-enrollment?product=sam`, `#search-results` | `Redeem access code` -> `#access-code-chooser` -> `MindTap`, `WebAssign`, or `SAM` -> `#help-article?article=mindtap-access-code`, `#help-article?article=webassign-access-code`, or `#help-article?article=sam-access-code`; WebAssign-scoped or SAM-scoped `Redeem an access code` -> `#help-article?article=webassign-access-code` or `#help-article?article=sam-access-code` | help-domain hub or search -> guided routing page -> help article; product-scoped help-domain -> help article | end-to-end | The product branches now land on real higher-ed access-code articles instead of static placeholders, and the WebAssign- and SAM-scoped access pages go straight to the honest product-specific articles. |
| Spark Dynamic Lessons article family | `#product-hub-spark`, `#search-results` | `Dynamic Lessons` -> `#help-article?article=spark-dynamic-lessons` -> Magma Tabs / next-previous controls across the family | product hub or search -> help article family | end-to-end | This is a clean, self-contained article-family pattern with direct sibling navigation. |

## B. Workflows that are partially complete but still acceptable to demonstrate

| Workflow | Starting surface | Path through the prototype | Destination type(s) | Review status | Note |
|---|---|---|---|---|---|
| Access your course through LMS | `#course-access-enrollment`, `#search-results`, product-hub LMS links | `Access your course through LMS` -> `#lms-access-chooser` -> LMS sign-in branch, LMS-link branch, or Spark LMS-access branch | help-domain hub or search or product hub -> guided routing page -> instruction-path page or guided routing page or help article | partial | The named LMS branches are now honest, but the not-sure branch remains intentionally unresolved. |
| Purchased access but course is unavailable | `#course-access-enrollment`, `#search-results` | `Purchased access but course is unavailable` -> `#purchased-access-chooser` -> join/enroll, wrong-account, LMS-access, or wrong-course branch | help-domain hub or search -> guided routing page(s) -> mixed downstream types | partial | The chooser now points to narrower truthful branches, but some downstream paths still end in unresolved states. |
| Wrong course or missing course | `#course-access-enrollment`, `#search-results` | `Wrong course or missing course` -> `#wrong-course-chooser` -> `transfer-or-drop-course`, `wrong-product-or-course-in-lms`, or another chooser | help-domain hub or search -> guided routing page -> help article or another chooser | partial | The strongest branches now land on real articles, but the full scenario family is not complete enough to call end-to-end. |
| Error messages, sync, or integration issues | `#troubleshooting-common-problems`, `#search-results`, product-hub LMS/integration links | `Error messages, sync, or integration issues` -> `#error-sync-chooser` -> grade sync, wrong LMS product/course, Spark institutional settings, or Spark LTI article | help-domain hub or search or product hub -> guided routing page -> help article | partial | Several real answer branches exist, but the not-sure branch remains intentionally non-interactive. |

## C. Workflows that should not be highlighted yet because they still dead-end or widen incorrectly

| Workflow | Starting surface | Path through the prototype | Destination type(s) | Review status | Note |
|---|---|---|---|---|---|
| SAM course-key entry | `#homepage`, `#course-access-enrollment`, `#course-access-enrollment?product=sam`, `#search-results` | `Enter course key` -> `#course-key-chooser` -> `SAM`; SAM-scoped `Enter a course key` stays static | help-domain hub or search -> guided routing page -> dead end; product-scoped help-domain -> static label | not review-ready | A truthful standalone SAM course-key answer object is still not in the prototype, so these branches remain intentionally static. |
| Missing activities or assignments for WebAssign, SAM, or not sure | `#homepage`, `#troubleshooting-common-problems`, `#troubleshooting-common-problems?product=webassign`, `#troubleshooting-common-problems?product=sam`, `#search-results`, `#product-hub-webassign`, `#product-hub-sam` | Homepage shortcut, Troubleshooting hub link, or `#search-results` result -> `#missing-activities-chooser` -> `WebAssign`, `SAM`, or `I'm not sure`; WebAssign-scoped `Missing activities or assignments` and WebAssign or SAM `Missing assignments` stay static | filtered shortcut or help-domain hub or search or product-scoped help-domain or product hub -> guided routing page -> dead end or static label | not review-ready | Only the MindTap and Spark branches now have truthful downstream articles, so the remaining branches stay intentionally unresolved. |
| Missing content or course materials | `#troubleshooting-common-problems`, `#troubleshooting-common-problems?product=webassign`, `#troubleshooting-common-problems?product=sam`, `#troubleshooting-common-problems?product=spark`, `#search-results`, product-hub static troubleshooting labels | `Missing content or course materials` -> `#missing-content-chooser` -> non-interactive product branch; WebAssign-scoped, SAM-scoped, or Spark-scoped `Missing content or course materials` stays static | help-domain hub or product-scoped help-domain or search -> guided routing page -> dead end | not review-ready | Product-specific missing-content destinations are still absent, so the prototype intentionally stops here. |
| Spark error messages, sync, or integration issues | `#troubleshooting-common-problems?product=spark` | Spark-scoped `Error messages, sync, or integration issues` stays static | product-scoped help-domain -> static label | not review-ready | The prototype does not yet have one Spark-scoped troubleshooting destination that is specific enough to trust for this broad issue label. |

## Known dead ends by design

- `#access-code-chooser`: `I'm not sure`
- `#course-key-chooser`: `SAM` and `I'm not sure`
- `#join-enroll-chooser`: `I'm not sure`
- `#lms-access-chooser`: `I'm not sure`
- `#lms-link-chooser`: `I'm not sure which product the LMS link opens`
- `#manage-account-chooser`: `I sign in to another product through my school or LMS` and `I'm not sure which account I use`
- `#manage-account-chooser?product=mindtap`: `I sign in to MindTap through my LMS or school portal` and `I'm not sure which account I use`
- `#manage-account-chooser?product=webassign`: `I sign in to WebAssign through my LMS` and `I'm not sure which account I use`
- `#manage-account-chooser?product=sam`: `I sign in to SAM through my LMS` and `I'm not sure which account I use`
- `#manage-account-chooser?product=spark`: `I'm not sure which Spark account I use`
- `#wrong-account-chooser`: `I access another product through my LMS or school portal` and `I'm not sure`
- `#wrong-account-chooser?product=mindtap`: `I access MindTap through my LMS or school portal` and `I'm not sure`
- `#wrong-account-chooser?product=webassign`: `I access WebAssign through my LMS` and `I'm not sure`
- `#wrong-account-chooser?product=sam`: `I access SAM through my LMS` and `I'm not sure`
- `#wrong-account-chooser?product=spark`: `I'm not sure`
- `#wrong-course-chooser`: `I'm not sure`
- `#missing-activities-chooser`: `WebAssign`, `SAM`, and `I'm not sure`
- `#missing-content-chooser`: all product branches and `I'm not sure`
- `#error-sync-chooser`: `I'm not sure`
- Product hubs: `Missing assignments` in the WebAssign and SAM troubleshooting areas remain static, and `Missing content` links in the MindTap and Spark troubleshooting areas remain static because truthful product-specific destinations do not exist yet.
- Spark-scoped `Troubleshooting & Common Problems`: `Missing content or course materials` and `Error messages, sync, or integration issues` remain static because truthful Spark-specific destinations do not exist yet.
